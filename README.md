# 📚 Personal Library Manager

A simple Java console application to help you manage a personal library of books. This project demonstrates core Java concepts like OOP, collections, file I/O, and user input processing.

Created to demonstrate Java fundamentals in a clean, practical project.

---

## ✨ Features

* **Add Book:** Enter new books with title, author, year, and ISBN.
* **View All Books:** List all stored books with their details.
* **Update Book:** Change any detail about a selected book.
* **Delete Book:** Remove a book from the library.
* **Search Books:** Find books by title or author keyword.
* **Save & Load:** Books are automatically saved to, and loaded from, `books.txt` for persistence.

---

## 💡 Concepts Demonstrated

* **Java Classes & Objects (OOP):** Using `Book` and `Library` classes to model real-world entities.
* **Encapsulation:** Bundling data and methods within classes.
* **Collections:** Using `ArrayList` to store and manage the list of books.
* **File I/O:** Using `BufferedReader`, `BufferedWriter`, `FileReader`, and `FileWriter` to read from and write to a `.txt` file.
* **Exception Handling:** Using `try-catch` blocks to gracefully handle errors like invalid input or file
